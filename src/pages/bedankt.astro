---
import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';
---

<Layout 
  title="Bedankt voor je bericht — Crystal Helder"
  description="Je aanvraag is ontvangen. Crystal neemt zo spoedig mogelijk contact met je op."
>
  <!-- DataLayer: Conversion event on thank you page -->
  <script is:inline>
  window.dataLayer = window.dataLayer || [];
  window.dataLayer.push({
    event: 'generate_lead',
    lead_source: 'kennismakingsformulier',
    conversion_page: true
  });
  </script>

  <div class="min-h-screen flex items-center justify-center px-6 py-24 relative overflow-hidden">

    {/* Background blobs */}
    <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
      <div class="absolute top-[-10%] right-[-5%] w-[400px] h-[400px] bg-[var(--color-brand-secondary)] rounded-full mix-blend-multiply filter blur-[80px] opacity-70"></div>
      <div class="absolute bottom-[-10%] left-[-5%] w-[350px] h-[350px] bg-[var(--color-brand-sage)] rounded-full mix-blend-multiply filter blur-[100px] opacity-50"></div>
    </div>

    <div class="max-w-2xl mx-auto text-center">

      {/* Animated checkmark circle */}
      <div class="w-24 h-24 rounded-full bg-[var(--color-brand-sage)] flex items-center justify-center mx-auto mb-10 shadow-lg shadow-[var(--color-brand-sage)]/30">
        <svg class="w-12 h-12 text-[var(--color-brand-primary-text)]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
        </svg>
      </div>

      <div class="inline-block px-4 py-2 rounded-full bg-white/80 backdrop-blur-md border border-white mb-6 text-xs font-semibold tracking-widest uppercase text-[var(--color-brand-accent)] shadow-sm">
        Aanvraag Ontvangen
      </div>

      <h1 class="text-4xl md:text-6xl font-bold mb-6 text-[var(--color-brand-primary-text)] leading-tight">
        Fijn dat je de <span class="text-[var(--color-brand-accent)]">stap</span> hebt gezet.
      </h1>

      <p class="text-lg md:text-xl text-[var(--color-brand-muted)] mb-4 leading-relaxed max-w-xl mx-auto">
        Je aanvraag is ontvangen. Crystal neemt zo snel mogelijk persoonlijk contact met je op — meestal binnen 1 à 2 werkdagen.
      </p>

      <p class="text-base text-[var(--color-brand-muted)] mb-12 max-w-lg mx-auto">
        Controleer ook je spammap voor het geval onze bevestigingsmail daar terechtkomt.
      </p>

      <div class="bg-white/70 backdrop-blur-xl border border-white rounded-3xl p-8 md:p-10 shadow-xl shadow-[var(--color-brand-muted)]/5 mb-12 text-left">
        <p class="text-xl md:text-2xl font-medium text-[var(--color-brand-primary-text)] leading-relaxed italic">
          "Samen vertalen we de taal van je lichaam."
        </p>
        <p class="mt-4 text-sm font-semibold text-[var(--color-brand-muted)]">— Crystal Helder, GNM Begeleider</p>
      </div>

      <Button href="/" className="text-lg px-10 py-5">
        Terug naar de homepagina
      </Button>

    </div>
  </div>
</Layout>
